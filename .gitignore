# Maven build output
target/

# Assembled demo install + published repo + local cache
dist/

# Local-only notes / scratch
.local/

# Signing keys — generated locally by tools/KeyGen.java, never committed.
# Globs, not exact names: per-environment keys are suffixed (…-prod, …-uat, …).
launch-signing-key*.b64
launch-verify-key*.b64
verify-key*.b64
*.pk8.b64

# IDE
.idea/
*.iml
.vscode/

# OS
Thumbs.db
.DS_Store
