# Apps this launcher carries directly, resolved without any download.
#   <app id> = <main class> ; <bundled version>
# The classes are shaded into the launcher's -app jar, so launching a bundled app
# just runs it from the launcher jar's own classpath.
hello = com.example.fxsuite.app.hello.HelloMain ; 1.0.0
